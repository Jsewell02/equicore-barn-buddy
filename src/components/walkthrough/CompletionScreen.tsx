import React from 'react';

// Simplified completion screen without navigation to prevent router context errors
export const CompletionScreen: React.FC<{ enterInteractiveMode: () => void }> = ({ enterInteractiveMode }) => {
  return null; // Disabled for now to prevent router context errors
};